# Required only for --mode live. Never commit a real key.
OPENAI_API_KEY=replace-with-your-key
OPENAI_MODEL=gpt-5.6-luna

# Optional user-supplied OpenAI-compatible endpoint; omit for the official endpoint.
# OPENAI_BASE_URL=
